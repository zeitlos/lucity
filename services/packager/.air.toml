root = "."
tmp_dir = "../../tmp/air/packager"
env_files = [".env"]

[build]
  bin = "../../tmp/air/packager/packager"
  cmd = "go build -o ../../tmp/air/packager/packager ./cmd/packager/..."
  delay = 1000
  exclude_dir = ["node_modules", "tmp", "vendor"]
  exclude_regex = ["_test\\.go$"]
  include_ext = ["go"]
  include_dir = [".", "../../pkg/auth", "../../pkg/packager", "../../pkg/graceful", "../../pkg/logger"]
  kill_delay = "2s"
  send_interrupt = true
  stop_on_error = true

[log]
  main_only = false
