type User {
  name: String
  email: String
  avatarUrl: String!
  workspaces: [WorkspaceMembership!]!
}

type WorkspaceMembership {
  workspace: String!
  role: WorkspaceRole!
}

enum WorkspaceRole {
  USER
  ADMIN
}

extend type Query {
  me: User! @hasRole(role: [USER])
}
