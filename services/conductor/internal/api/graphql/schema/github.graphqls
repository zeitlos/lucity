type GitHubRepository {
  id: ID!
  name: String!
  fullName: String!
  htmlUrl: String!
  defaultBranch: String!
  private: Boolean!
}

type GitHubInstallation {
  accountLogin: String!
  accountAvatarUrl: String!
  accountType: GitHubAccountType!
}

enum GitHubAccountType {
  ORGANIZATION
  USER
}

extend type Query {
  """User's accessible GitHub App installations. Requires connected GitHub account."""
  githubSources: [GitHubInstallation!]! @hasRole(role: WORKSPACE_MEMBER)
  """Repos accessible to the user under a GitHub account (org or user login)."""
  githubRepositories(account: String!): [GitHubRepository!]! @hasRole(role: WORKSPACE_MEMBER)
  """Whether the current user has connected their GitHub account."""
  githubConnected: Boolean! @hasRole(role: WORKSPACE_MEMBER)
}
