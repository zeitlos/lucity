JWT_SECRET=change-me-in-production

# Classic PAT with write:packages scope for pushing images to GHCR.
# GitHub App OAuth tokens cannot push to GHCR — this is a known GitHub limitation.
# Create one at: https://github.com/settings/tokens/new?scopes=write:packages
REGISTRY_TOKEN=ghp_your_token_here
