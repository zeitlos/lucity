{{- if .Values.routes.enabled }}
{{- $responseHeaders := .Values.routes.responseHeaders }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "lucity.fullname" . }}-platform
  labels:
    {{- include "lucity.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ .Values.routes.gatewayName }}
      namespace: {{ .Values.routes.gatewayNamespace }}
  hostnames:
    - {{ .Values.routes.hostname | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /graphql
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /auth
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /playground
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /version
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /app
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        {{- with $responseHeaders }}
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
        {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-dashboard
          port: 80
    {{- if .Values.cashier.enabled }}
    - matches:
        - path:
            type: PathPrefix
            value: /webhooks/stripe
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-cashier
          port: {{ .Values.cashier.webhookPort }}
    {{- end }}
    - matches:
        - path:
            type: PathPrefix
            value: /webhooks
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 9004
    - matches:
        - path:
            type: PathPrefix
            value: /api
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-conductor
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      {{- with $responseHeaders }}
      filters:
        {{- include "lucity.responseHeaderFilter" . | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "lucity.fullname" . }}-docs
          port: 80
{{- end }}
