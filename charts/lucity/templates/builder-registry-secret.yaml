{{- if and .Values.conductor.buildkit.enabled .Values.secrets.REGISTRY_PASSWORD }}
{{- $ns := .Values.conductor.buildNamespace | default "lucity-builds" }}
{{- $host := .Values.conductor.registryHost | default "lucity-infra-zot.lucity-system.svc.cluster.local:5000" }}
{{- $user := .Values.conductor.registryUsername | default "builder" }}
{{- $pass := index .Values.secrets "REGISTRY_PASSWORD" }}
{{- $auth := printf "%s:%s" $user $pass | b64enc }}
{{- $dockerConfig := printf "{\"auths\":{\"%s\":{\"auth\":\"%s\"}}}" $host $auth }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lucity.fullname" . }}-registry-auth
  namespace: {{ $ns }}
  labels:
    {{- include "lucity.labels" . | nindent 4 }}
    app.kubernetes.io/component: builder
type: Opaque
stringData:
  config.json: {{ $dockerConfig | quote }}
{{- end }}
