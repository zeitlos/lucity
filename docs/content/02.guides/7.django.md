---
title: Django
description: Deploy a Django project on Lucity, with migrations, static files, and PostgreSQL.
---

::note
This page is still being written.
::

Planned content:

- What Railpack detects in a Django project
- Gunicorn, workers, and the start command
- Running `migrate` and `collectstatic` on every release
- Serving static and media files from an object storage bucket
- `ALLOWED_HOSTS`, `CSRF_TRUSTED_ORIGINS`, and the generated domain
