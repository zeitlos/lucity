---
title: Variables
description: "Configuration for your services: service variables, shared variables, and dynamic variables."
---

::note
This page is still being written.
::

Planned content:

- Service variables, and how they reach the running container
- Shared variables applied across every service in an environment
- Dynamic variables: linking to a database, key-value store, or bucket instead of pasting credentials
- Which references each resource offers
- Why a variable change rolls the service out without rebuilding it
- Secrets, and what is visible in the dashboard
